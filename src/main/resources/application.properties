# Application Name
spring.application.name=StudentRegistration

# MySQL Connection (Docker container name used instead of localhost)
spring.datasource.url=jdbc:mysql://mysql:3306/studentdb?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=lakshmi123

# Server Port
server.port=8089

# Hibernate & JPA Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false

# Error Page
server.error.whitelabel.enabled=false

# Thymeleaf (Optional - disable cache in dev)
spring.thymeleaf.cache=false


# Optional: Base path if you're using external links
# api.base.path=http://localhost:8089

